<%= react_component('CleanNavbar', {
      root_path: root_path,
      language_ko: ENV.fetch("CHATBOT_LANGUAGE_KO") { '루비' },
    }) %>

<%= react_component('AccountConnection',{
      connection_code: params[:code],
      pusher_key: Rails.application.credentials.dig(:pusher, :key).to_s,
      header: '계정 인증 코드',
      text: "비밀번호 재설정을 위하여, '#{ENV.fetch("CHATBOT_LANGUAGE_KO") { '루비' }} 챗봇' 텔레그램 봇에게 아래의 코드를 전송하세요.",
      redirect_path: edit_user_password_path
    }) %>
