<%= react_component('AccountConnection',{
      connection_code: current_user.connection_code,
      pusher_key: Rails.application.credentials.dig(:pusher, :key).to_s,
      header: '계정 연결 코드',
      text: "텔레그램 계정 연결을 위하여, '#{ENV.fetch("CHATBOT_LANGUAGE_KO") { '루비' }} 챗봇' 텔레그램 봇에게 아래의 코드를 전송하세요.",
      redirect_path: bots_path
    }) %>
